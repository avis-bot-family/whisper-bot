

# Feature: AI Summary Generator на основе транскрибации и диаризации
1. Цель фичи
Автоматизировать создание структурированных meeting summary на основе транскрибированных аудио с диаризацией, получаемых от Telegram-бота.
Входные данные: Текст с таймкодами и спикерами
Выходные данные: Структурированный summary на русском языке в роли Tech Lead backend

## Роль и контекст
Ты — технический лидер backend-направления с экспертизой в Python/Perl, микросервисах и highload.
Твоя задача: анализировать транскрибацию встречи и создавать структурированный executive summary.

## Входные данные
- Дата: {meeting_date}
- Участники: 
{participants_formatted}
- Контекст: {context_hints}

## Требования к output
1. Язык: строго русский, технический стиль, без "воды"
2. Структура:
   - **Основная тема**: 1 предложение, суть встречи
   - **Ключевые решения**: список с заголовками и пояснениями
   - **Технические детали**: архитектурные изменения, таблицы, API
   - **Задачи (если есть)**: что, кто, когда
   - **Открытые вопросы**: что требует уточнения

3. Принципы:
   - Говори по делу, но с достаточным контекстом для понимания
   - Сохраняй технические термины (catalog_type_id, action_id, etc.)
   - Если спикер упоминает "временно", "на старте", "промежуточное решение" — отмечай это
   - Не выдумывай факты, которых нет в транскрибации
   - Если информация противоречива — отметь это в "Открытых вопросах"

## Формат ответа
Верни ТОЛЬКО валидный JSON согласно схеме SummaryResult.
Никакого markdown, никакого pre-text. Только JSON.


📋 Summary: Backend Planning 2026-Q1

🎯 Основная тема:
Планирование работ на 1-й квартал 2026 года...

✅ Ключевые решения:
• Доступ к плейлистам: полный доступ в рамках музыкальных баз...
• Копирование расписаний: реализация full-copy в рамках типа...

⚙️ Технические детали:
• catalog_type_id: добавлен в music.bases (SMALLINT)...
• Публикатор: выбор action_id по типу расписания...

📌 Задачи:
• [HIGH] История плейлистов → к началу февраля @Ярослав

❓ Открытые вопросы:
• Уточнить требования по автопролонгации договоров


[x] 1. Создать Pydantic-схемы (SummaryRequest, SummaryResult)
[x] 2. Реализовать SummaryGenerator с async LLM-клиентом
[x] 3. Настроить prompt templating с подстановкой метаданных
[x] 4. Добавить retry-логику и JSON fallback
[ ] 5. Интегрировать в Telegram bot handler (/summary command)
[ ] 6. Покрыть unit-тестами + mock LLM responses
[ ] 7. Добавить логирование и метрики (время генерации, ошибки)
[ ] 8. Документировать API endpoint (если будет REST)

## Реализовано (скрипт для проверки)

**Ollama (локально):**
```bash
# 1. Запустить Ollama (модель скачивается при старте)
make ollama-up

# 2. Опционально в .env:
# summary_BASE_URL=http://localhost:11434/v1  (по умолчанию)
# summary_MODEL=llama3.2
```

**Использование:**
```bash
# Вариант 1: из готовой транскрибации (быстро)
poetry run python src/scripts/generate_summary_local.py \
  --transcription src/bot/audio/sample_transcription.txt

# Вариант 2: полный пайплайн (аудио → транскрибация + диаризация → summary)
poetry run python src/scripts/generate_summary_local.py path/to/meeting.mp3
```
